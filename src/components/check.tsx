import { CheckIcon } from "../icons/icons";

export default function Check() {
    return (
        <div className="size-6 flex items-center text-white justify-center p-1 rounded-full bg-accent">
            <CheckIcon />
        </div>
    )
}
